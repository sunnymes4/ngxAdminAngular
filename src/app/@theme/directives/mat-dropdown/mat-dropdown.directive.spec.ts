import { MatDropdownDirective } from './mat-dropdown.directive';

describe('MatDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new MatDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
